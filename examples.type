Script: 
$$('h2').filter(node => node.textContent.startsWith('TS')).map(node => node.textContent.slice(2))

`This condition will always return false since the types ConnectorScopeType | undefined and "asd" have no overlap.`;

type Person = {
  age: number;
  name: string;
  name2: string;
  name3: string;
  name4: string;
  name5: string;
  name6: string;
};

const benny: Person = {};
